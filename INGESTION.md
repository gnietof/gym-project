# Ingestion

This file describes the steps required to ingest all the information in the database. 
This application uses mainly two tables: activities and sessions. Activities stores information about all the different activities while sessions keeps a list of when those activities are scheduled in the Gym.

## Changes in V2.0 
The structure of the tables in the database has slightly changed in V2.0. 
Initially, all the information about the activities was stored in one single table which included all the data and also the 'full_activity' column and the embeddings. The full_activity was a merge of all the semantically searchable information. This information had been embedded and stored in the same table.

In V2.0, the activities table only keeps information about the activities (including some fields which initially where only present in the full_activity column.
The 'full_activity' and the embedding columns have been moved to a different table.

## Plan for V2.1
The 'usages' table keeps track of all the information sent to the LLM's. This table includes lots of information. It also includes the scores being provided by the users to each of the AI assistant responses and the number of tokens used in each request.
In V2.1 the scores and the tokens will also be moved to different tables. While the 'usages' is basically a log, 'scores' and 'token' are used to build the dashboards so they will be accessed more frequently. This separation will improve performance.

## Tables

| Schema | Table | Description |
| - | - | - |
| gym | activities | List of all the different guided activities available at the gym. |
| gym | sessions | Schedule of each of the activities along the week. |
| gym | categories | List of categories for each of the activities. |
| gym | subcategories | List of subcategories for each of the activities. |
| gym | intensity_levels | All the intensity levels for each of the activities. |
| gym | impact_levels | All the impact levels for each of the activities. |
| gym | skill_levels | All the impact levels for each of the activities. |
| gym | caloric_burns | All the caloric burn levels for each of the activities. |

Additionally there is a table which includes questions for evaluating the system but is not part of the solution.

| Schema | Table | Description |
| - | - | - |
| gym | questions | List of random questions for testing LLM accuracy. |

The prompts table allows auditing and verstioning the prompts used when communicating with the LLM.

| Schema | Table | Description |
| - | - | - |
| llm | prompts | The prompts which are being used for each of the LLM requests. | 

Finally there is a table which keeps a record of all the requests being sent to the LLM's for further analysis.

| Schema | Table | Description |
| - | - | - |
| log | usages | Keeps track of all the requests being sent to the LLM's and the received responses. |

## Initial Load

All the information comes initially from three JSON files:

| File | Description |
| - | - |
| GymActivities | All information related to each of the activities. This file was generated by ChatGPT. |
| activities | Specific information for each of those activities in the gym such as duration. |
| sessions | Initial schedule of each of the sessions. |

### Ingestion process

1. All the tables (activities, sessions, impacts ...) as well as the temporary tables are created in the database using [create.sql](init/create.sql).
2. The temporary tables being created are:
-  All the information in GymActivities loaded in table tmp_descriptions.
-  All the information in activities loaded in table tmp_activities.
-  All the information in sessions loaded in table tmp_sessions.
Tables tmp_activities and tmp_sessions are related by a code which was made up when the initial schedule was encoded in an JSON file.
Tables tmp_descriptions and tmp_activities are related by the activity_name.
Python script [load.py](init/load.py) is used to ingest these JSON files into the tables.
3. For intensity, impact, skills and caloric burns table contents are generated using [init.sql](init/init.sql). Each of the values found in GymActivities have been included.

4. Category and subcategories tables are filled with unique values in the tmp_descriptions using a SQL instruction.
5. The final activities table is filled using the information from tmp_descriptions using [fill.sql](init/fill.sql).
- Each category and subcategory is converted into a code (matching the related tables).
- Each intensity, impact, skill and caloric burn is converted into a code (matching the related tables).
- An activity_code is added from the tmp_activities table.

6. The embeddings table is filled. This is a two step process:
- First, filling the activity_code, activity_name and also the content column with a 'document' generated by merging several tmp_activities columns. This was already done when executing the previous script [fill.sql](init/fill.sql).
- Then, using another Python script [embed.py](init/embed.py) generate the embeddings for each of the documents and stores them in this table.

**** PENDING!
7. The sessions table is filled with the code of the activity, dow (sunday to saturday), day (sunday = 0 to saturday = 6), the start time and the duration (which initially was in the tmp_activities; this will allow the same activity have sessions scheduled with different durations).

**** Esta vista se podría crear junto con las tablas y ya está
8. Create a view which connects activities, intensities, skills, impacts and caloric burns.
